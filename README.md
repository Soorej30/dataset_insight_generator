# dataset_insight_generator
Automated Dataset Insight Generator

run ```pip install requirements.txt```

Once completed, we run the streamlit app using the command

```streamlit run app.py```

This will open up the browser with the website where you will follow the instructions to upload the data file and the data dictionary. 

Alternatively, you can visit the hosted website https://dataset-insight-generator.streamlit.app/